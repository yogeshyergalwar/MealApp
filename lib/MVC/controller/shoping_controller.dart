/*
import 'package:get/get.dart';
import 'package:untitled5/model/product.dart';

class ShopingController extends GetxController {
  var product = <Product>[].obs;

  @override
  void onInit() {
    // TODO: implement onInit
    fetchApi();
    super.onInit();
  }

  void fetchApi() async {
    await Future.delayed(Duration(seconds: 1));
    var producrtResult = [
      Product(1, 'Cloths', 'great color and best fit under 100', 'ProductImage',
          99.0),
      Product(2, 'Dress', 'great color and best fit under 100', 'ProductImage',
          99.0),
      Product(3, 'shoes', 'great color and best fit under 100', 'ProductImage',
          99.0),
      Product(4, 'accessories', 'great color and best fit under 100',
          'ProductImage', 99.0),
    ];
    product.value = producrtResult;
  }
}
*/
